"""
Application feature modules.

Each module (e.g. `auth`, `users`) groups together its router,
service wrapper, and schemas for better structure. Routers here
can either define routes directly or include the routers from
`app.api.*` to maintain backward compatibility while refactoring.
"""

